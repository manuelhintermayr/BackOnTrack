<UserControl x:Class="BackOnTrack.UI.MainView.Pages.Settings.SettingsGeneral"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BackOnTrack.UI.MainView.Pages.Settings"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer>
            <StackPanel x:Name="Panel" MinWidth="150">
                <TextBlock Text="GENERAL" Style="{StaticResource Heading2}" Margin="0,0,0,8" />
                <mui:BBCodeBlock xml:space="preserve" BBCode="This is the overall general configuration for the program. Changes here will be made only for the current user.
                                 
                                 [b]Changes are only active after the save button has been pressed.[/b]
                                 " Margin="0,0,0,16"/>

                <Grid HorizontalAlignment="Left">
                    <CheckBox Content="Start application with System start" Margin="0,0,0,4" IsChecked="{Binding Path=AutoRunEnabled}" />
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
